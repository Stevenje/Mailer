// Generated by CoffeeScript 1.7.1
var app, bodyParser, cors, express, http, logger, profiles;

express = require("express");

bodyParser = require("body-parser");

http = require("http");

cors = require("cors");

logger = require("morgan");

profiles = require("./routes/profiles2");

app = express();

app.use(logger("dev"));

app.use(bodyParser());

app.use(cors());

app.get("/profiles", profiles.findAll);

app.put("/profiles/:id", profiles.updateProfile);

app.post("/email", profiles.sendEmail);

app.post("/search", profiles.search);

app.post("/comp", profiles.comp);

app.listen(3000);

console.log("Listening on port 3000 ;)");
